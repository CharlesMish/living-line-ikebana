{
  "name": "living-line-ikebana",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "test": "node tools/run-tests.mjs",
    "build": "vite build && node tools/build-standalone.mjs",
    "verify": "npm run typecheck && npm test && npm run build && node tools/validate-dist.mjs"
  },
  "dependencies": {
    "three": "0.181.2"
  },
  "devDependencies": {
    "@types/three": "0.185.4",
    "esbuild": "0.28.2",
    "typescript": "5.9.3",
    "vite": "7.3.6"
  }
}
